[test]
coverage = true
coveragePathIgnorePatterns = [
  "test/**",
  "src/lib/api-client/gen/**",
]
# Aggregate minimums (%). Enforced in CI; see .github/workflows/app-pr-checks.yml.
coverageThresholds = { lines = 96, functions = 90 }