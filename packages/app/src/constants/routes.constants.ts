export enum Routes {
  LOGIN = "/",
  CARDS = "/cards",
  CARD_CODE = "/card-code",
  CARD_SETTINGS = "/card-settings",
  SELECT_BRAND = "/select-brand",
  SCAN = "/scan",
  SCAN_MANUAL_ENTRY = "/scan-manual-entry",
  SETTINGS = "/settings",
}
