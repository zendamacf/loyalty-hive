export enum Routes {
  LOGIN = "/",
  CARDS = "/cards",
  CARD_CODE = "/card-code",
  CARD_SETTINGS = "/card-settings",
  SELECT_BRAND = "/select-brand",
  SCAN = "/scan",
  SETTINGS = "/settings",
}
